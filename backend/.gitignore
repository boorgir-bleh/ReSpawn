target/
*.log
.idea/
*.iml
.vscode/
.DS_Store

# .env.local: still gitignored for genuinely personal/per-machine overrides.
# run-local.ps1 is intentionally NOT ignored - it holds the team's shared
# OTP-sender Gmail credentials (cafeotp.noreply@gmail.com), committed on purpose.
.env.local
